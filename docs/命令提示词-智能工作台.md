# constitution命令提示词-智能工作台
1.后端接口全部使用node.js开发，前端使用react开发, 前端调用后端接口
2.前端组件全部使用 less + antd开发
3.图标组件全部使用echarts开发
4.数据库使用mysql
5.鉴权方案固定使用JWT
6.QQ邮箱连接方案固定使用SMTP/IMAP（授权码通过环境变量配置）

## 登录/注册/会话保持/登出与未登录拦截

### specify 命令提示词（第一个功能：登录/注册）

请为「智能工作台助手」创建第一个功能规格：实现基本的登录/注册能力。
1.范围仅包含注册、登录、会话保持、登出与未登录拦截，不包含忘记密码、邮箱验证、第三方登录。
系统约束：
1.系统约束：后端使用 Node.js 提供接口，前端使用 React 调用接口，
2.前端样式与组件采用 Less + Ant Design，数据库使用 MySQL 存储用户与会话数据。请严格对齐 `docs/智能工作台助手-需求分析.md` 中 User Story 0（登录与账号）、功能需求 FR-A01 至 FR-A05、关键实体 User/Session、Edge Cases（用户名重复、密码强度校验、会话过期、后端或数据库错误提示）与成功标准 SC-00。验收场景至少覆盖：1）未登录访问工作台功能时跳转登录页；2）登录成功后进入工作台并获得会话标识；3）注册时校验唯一性并写入用户表；4）刷新页面保持登录；5）登出后清除会话并返回登录页。请输出清晰、可测试、可直接进入 plan 阶段的 spec。


### plan部分---技术相关的文档

请基于 `specs/001-auth-login-register/spec.md` 生成本功能的技术实现计划（plan），目标是落地“登录/注册”MVP。请输出可执行的技术方案，覆盖架构、数据模型、接口、任务拆分与风险控制，且与 `docs/智能工作台助手-需求分析.md` 保持一致。

技术约束（必须遵守）：
1. 后端：Node.js + Express。
2. 前端：React。
3. UI：Less + Ant Design。
4. 数据库：MySQL。
5. 前端仅调用后端 API，不直接连接数据库。
6. 鉴权方案固定：JWT（不再做 Session 二选一）。
7. QQ 邮箱连接方案固定：SMTP/IMAP；授权码通过环境变量注入（例如 `QQ_MAIL_AUTH_CODE`），不得写入代码或文档明文。

计划内容至少包含：
1. 架构与目录：前后端模块划分、建议目录结构、环境变量清单（如 DB、JWT、端口）。
2. 数据模型：`users`、`sessions`（或等价会话方案）表结构草案，字段、索引、唯一约束（如 username/email）。
3. API 设计：注册、登录、登出、获取当前用户信息（用于登录态恢复）四类接口；请求/响应示例；错误码与错误信息规范。
4. 鉴权方案：使用 JWT；说明 access token 存放方式、过期策略、刷新页面保持登录、未登录拦截的实现路径。
5. 安全要求：密码哈希（bcrypt）、基础输入校验、敏感信息不落日志、最小权限原则。
6. 前端流程：登录页/注册页表单校验、提交与错误提示、登录成功跳转、路由守卫、登出流程。
7. 测试计划：最小测试集合（注册成功、重复用户名、登录失败、登录态恢复、登出失效）。
8. 任务拆分：按“后端先行 -> 前端接入 -> 联调 -> 测试”给出可执行 tasks，并标注优先级和依赖关系。
9. 风险与决策：列出关键风险（如会话策略、跨域、QQ 邮箱后续集成兼容性）及规避方案。

范围边界（本阶段不做）：
1. 忘记密码、邮箱验证、第三方登录。
2. 多租户与复杂权限系统。
3. 邮件/日程/会话业务功能实现（本 plan 只做登录注册基础能力）。

## 2.首页实现
### specify 命令提示词（第二个功能：首页实现）

请为「智能工作台助手」创建第二个功能规格：实现登录后首页的“日程与待办管理 + AI 辅助待办”能力。

范围（本次必须包含）：
1. 登录后进入首页，展示今日/本周待办与日程概览（菜单），目前先有日程与待办，会话三个菜单，会话菜单页面暂时为空就行。
2. 待办基础能力：新增、列表展示、标记完成/未完成、删除、可选编辑。
3. 日程基础能力：新增、列表展示、编辑、删除（至少支持日期维度展示）。
4. 数据由后端 Node.js + Express 提供 API，持久化到 MySQL，并与当前登录用户绑定（JWT 鉴权）。
5. 接入阿里云百炼（OpenAI 兼容模式）实现 AI 能力：
   - 根据用户输入生成待办列表草稿；
   - 根据用户指令完成待办项（如批量标记完成）。

系统约束（必须遵守）：
1. 前端：React + Less + Ant Design。
2. 后端：Node.js + Express。
3. 数据库：MySQL。
4. AI 调用：`openai` SDK + DashScope OpenAI 兼容模式，`baseURL=https://dashscope.aliyuncs.com/compatible-mode/v1`，模型默认 `qwen-plus`。
5. 敏感信息仅通过环境变量注入（如 `DASHSCOPE_API_KEY`），不得在代码与文档中写明文密钥。

请严格对齐 `docs/智能工作台助手-需求分析.md` 中以下内容：
1. User Story 2（日程与待办管理）。
2. 3.2 日程与待办模块（FR-T01 ~ FR-T08）。
3. 3.4 工作台整体（FR-W01，必要时参考 FR-W02）。
4. Edge Cases 中与日程/待办及 AI 使用相关的边界场景。
5. 成功标准中与待办、首页使用体验相关条目（至少覆盖 SC-03，并补充 AI 待办生成/完成的可测目标）。

验收场景至少覆盖：
1. 已登录用户进入首页可看到待办/日程列表与概览。
2. 用户可新增、完成、删除待办，刷新后数据仍正确（MySQL 持久化）。
3. 用户可新增并查看日程，支持基础编辑/删除。
4. 用户输入自然语言后，AI 可返回可落地的待办建议列表。
5. 用户发出“完成某些待办”的指令后，系统可正确更新对应待办状态。
6. AI 或后端异常时，前端提供清晰错误提示与重试入口。

范围边界（本次不做）：
1. QQ 邮箱收发功能实现。
2. 完整聊天中心/多会话管理。
3. 复杂日历视图（如月视图拖拽）、重复规则高级配置。

请输出清晰、可测试、可直接进入 plan 阶段的 spec，并避免写具体代码实现细节。

### plan部分---技术相关的文档（第二个功能：首页实现）

请基于 `specs/002-schedule-todo-management/spec.md` 生成本功能的技术实现计划（plan），目标是落地“首页日程与待办管理 + AI 辅助待办”MVP。请输出可执行的技术方案，覆盖架构、数据模型、接口、任务拆分与风险控制，且与 `docs/智能工作台助手-需求分析.md` 保持一致。

技术约束（必须遵守）：
1. 后端：Node.js + Express。
2. 前端：React。
3. UI：Less + Ant Design。
4. 数据库：MySQL。
5. 鉴权：JWT（沿用第一阶段）。
6. AI：阿里云百炼 OpenAI 兼容模式（`openai` SDK，`baseURL=https://dashscope.aliyuncs.com/compatible-mode/v1`，默认模型 `qwen-plus`）。
7. 前端仅调用后端 API，不直接连接数据库，不直接暴露 AI Key。
8. 图标与可视化组件可使用：ECharts、Ant Design 图标，或其他轻量级组件（优先体积小、维护成本低的方案）。

计划内容至少包含：
1. 架构与目录：前后端模块划分；首页菜单结构（先有日程/待办、会话菜单，其中会话页可占位）；环境变量清单（DB、JWT、DASHSCOPE_API_KEY、端口等）。
2. 数据模型：`todos`、`schedules` 表结构草案（含 `user_id` 关联）、状态字段、时间字段、索引设计；可选 AI 任务日志表。
3. API 设计：待办 CRUD、日程 CRUD、首页概览接口、AI 待办生成接口、AI 批量完成待办接口；请求/响应示例与错误码规范。
4. AI 流程设计：自然语言输入 -> 提取结构化待办建议 -> 用户确认 -> 写入/更新待办；失败重试与降级策略（AI 不可用时仍可手动管理待办）。
5. 前端流程：首页布局、待办与日程列表交互、AI 输入区与结果确认交互、异常提示与重试、空状态与加载态。
6. 组件策略：说明何处用 Antd 基础组件，何处用图标/轻量图表组件（ECharts/Antd/其他轻量级库）及选择理由。
7. 测试计划：最小测试集合（登录态访问控制、待办新增/完成/删除、日程新增/编辑/删除、AI 生成待办、AI 完成待办、异常场景）。
8. 任务拆分：按“后端接口 -> 前端页面 -> AI 接入 -> 联调测试”给出可执行 tasks，标注优先级与依赖。
9. 风险与决策：列出关键风险（AI 结果不稳定、误完成待办、跨域、性能）及规避方案（确认步骤、幂等、限流、日志）。

范围边界（本阶段不做）：
1. QQ 邮箱收发功能实现。
2. 完整聊天中心/多会话管理。
3. 高级日历能力（拖拽排程、复杂重复规则、团队协作）。

请确保输出结果能直接进入 `/speckit.tasks` 阶段，并给出“先做哪三件事”的执行建议。

## 4. AI 进阶会话（模型选择、历史记录与导出）
### specify 命令提示词（第四个功能：AI 进阶会话）

请为「智能工作台助手」创建第四个功能规格：实现 AI 会话的进阶能力，包含模型切换、历史会话管理及内容导出。

范围（本次必须包含）：
1. **模型切换**：用户可以在会话界面或设置中选择不同的阿里云百炼模型（qwen-plus, qwen-max, qwen-turbo），并即时生效；
2. **历史会话管理**：
   - 后端新增 `ai_sessions` 和 `ai_messages` 表，持久化存储用户的对话记录；
   - 前端提供历史会话列表，支持查看、切换和删除历史对话；
   - 对话按用户隔离，访客用户仅支持本地/临时存储（可选）。
3. **内容导出**：
   - 支持在单一 AI 回复框点击“导出”按钮；
   - 导出格式固定为 Markdown (.md)；
   - 导出文件名建议包含时间戳和对话摘要。

系统约束（必须遵守）：
1. 前端：React + Ant Design (使用 Select 组件选择模型，List/Menu 展示历史)；
2. 后端：Node.js + Express + MySQL；
3. 数据一致性：切换模型不影响当前对话上下文（除非重新开始会话）。

请严格对齐 `docs/智能工作台助手-需求分析.md` 中 FR-C06 至 FR-C08 需求。

验收场景：
1. 用户切换模型为 qwen-max 后，后续 AI 回复显示来自该模型；
2. 用户刷新页面后，能在侧边栏看到之前的对话历史并点击恢复；
3. 用户点击导出按钮，浏览器成功下载包含回复内容的 .md 文件。

### plan部分---技术相关的文档（第四个功能：AI 进阶会话）

请基于需求规格生成本功能的技术实现计划（plan），目标是落地“AI 进阶会话”功能。

技术约束（必须遵守）：
1. 后端：扩展 `ai.routes.js` 和 `ai.service.js`；
2. 数据库：设计 `ai_conversations` (id, user_id, title, model, created_at) 和 `ai_messages` (id, conv_id, role, content, created_at)；
3. 导出实现：前端使用 `Blob` 和 `URL.createObjectURL` 实现纯前端导出，或后端生成文件流。

计划内容至少包含：
1. 数据模型：详细设计历史记录表的字段、外键关系和索引；
2. API 设计：
   - `GET /api/ai/conversations`：获取会话列表；
   - `GET /api/ai/conversations/:id`：获取特定会话的消息详情；
   - `POST /api/ai/chat`：更新此接口以接收 `model` 参数并保存消息到数据库。
3. 前端流程：
   - 模型选择器的 UI 位置及状态持久化；
   - 历史会话列表的加载与切换逻辑；
   - Markdown 导出功能的实现细节（字符串拼接 -> Blob 下载）。
4. 任务拆分：按“数据库迁移 -> 后端接口 -> 前端 UI -> 联调测试”拆分 tasks。

### 5：MCP 基础框架
**目标**：在后端增加 MCP Tool Adapter，不改变现有接口行为。  
**关键要求**：
- 新增工具注册中心与执行器。
- 聊天接口支持工具调用循环（最多 N 轮，防止死循环）。
- 工具调用失败自动降级。
- 增加调用日志表或日志字段（会话ID、工具名、耗时、状态）。

#### specify 命令提示词

请为“个人生活助手”项目编写一份 **MCP 基础框架** 的需求规格（specify），并输出结构化的需求文档。

背景与目标：
1. 项目当前已接入千问模型，聊天主流程可用；
2. 本次仅做 MCP 基础框架，不改变现有 API 对外行为；
3. 目标是在后端建立可扩展的 Tool Adapter，为后续接入 12306、麦当劳等 MCP 做准备。

范围（In Scope）：
1. 工具注册中心（tool registry）；
2. 工具执行器（tool executor）；
3. 聊天接口的工具调用循环（最多 N 轮，防死循环）；
4. 工具失败自动降级到纯模型回答；
5. 工具调用日志（会话ID、工具名、参数摘要、耗时、状态、错误信息）。

非范围（Out of Scope）：
1. 具体业务 MCP（12306、麦当劳）的详细接入；
2. 下单、支付等高风险写操作；
3. 前端大改版。

技术约束（必须遵守）：
1. 后端技术栈保持 Node.js + Express；
2. 不破坏现有登录、待办、日程、AI对话功能；
3. 所有第三方凭据通过环境变量注入；
4. 必须支持超时、重试与降级；
5. 仅允许调用白名单工具。

请输出内容至少包含：
1. 业务流程与时序（用户请求 -> 模型决策 -> 工具调用 -> 回灌 -> 回复）；
2. 功能需求列表（FR 编号）与非功能需求（NFR）；
3. 数据与日志字段设计；
4. API/模块边界与接口契约；
5. 验收标准（AC）与回归测试范围；
6. 风险与回滚策略。

---

#### plan 命令提示词

请基于已确认的 MCP 基础框架需求规格，生成可执行的技术实现计划（plan）。

计划目标：
1. 在不影响现有功能的前提下完成 MCP Tool Adapter 落地；
2. 可通过配置开关灰度发布；
3. 为下一阶段接入 12306、麦当劳 MCP 预留扩展点。

实现约束：
1. 后端保持现有目录结构与代码风格；
2. 不修改前端核心交互，仅保留兼容；
3. 先实现查询类能力，禁止自动写操作；
4. 工具调用失败必须自动降级；
5. 每一步都要给出验证方式。

计划输出要求（必须包含）：
1. 分阶段任务拆分（Phase A/B/C）：
   - A：工具注册中心 + 执行器 + 调用循环；
   - B：日志与观测、超时重试、白名单校验；
   - C：灰度发布、回归测试、上线与回滚。
2. 每个任务的：
   - 目标与产出物；
   - 涉及文件/模块；
   - 风险点与规避措施；
   - 完成定义（DoD）。
3. 测试计划：
   - 单元测试、集成测试、回归测试用例清单；
   - 关键验收场景（工具成功、工具超时、工具异常、降级成功）。
4. 部署计划（Docker）：
   - 环境变量清单；
   - 灰度开关策略；
   - 监控与告警指标（成功率、失败率、平均耗时）。

请确保输出格式清晰，可直接转为开发任务清单执行。