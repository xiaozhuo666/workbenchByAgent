describe("GET /api/auth/me contract", () => {
  test.todo("returns 200 with current user payload when token/session valid");
  test.todo("returns 401 with AUTH_TOKEN_MISSING when authorization header absent");
});
