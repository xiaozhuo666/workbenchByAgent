describe("POST /api/auth/login contract", () => {
  test.todo("returns 200 with { code, message, data.token, data.user } on valid credentials");
  test.todo("returns 401 with AUTH_INVALID_CREDENTIALS on invalid password");
});
